# Copy this to .env and fill in your values
OPENAI_API_KEY=sk-...
GOAL=Harden the system, install monitoring tools, and report status.

# (Optional) Specify a non-root user for the agent to run its commands as.
# If not set, defaults to root (which will use sudo for all commands).
AGENT_USER=agent
